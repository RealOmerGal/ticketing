import { EventNames } from './event-names';
import { Event } from './event';
interface Client {
    publish(eventName: string, data: string, callback: (err: Error | undefined, guid: string) => void): void;
}
export declare abstract class Publisher {
    abstract eventName: EventNames;
    private _client?;
    setClient(client: Client): void;
    private get client();
    publish(event: Event<any>): Promise<string>;
}
export {};
