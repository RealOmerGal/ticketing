import { Event } from './event';
import { EventNames } from './event-names';
export interface ChargeCreatedData {
    id: string;
    orderId: string;
    amount: number;
}
export declare class ChargeCreatedEvent extends Event<ChargeCreatedData> {
    eventName: EventNames;
}
