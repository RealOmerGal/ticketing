import { Event } from './event';
import { EventNames } from './event-names';
export interface TicketUpdatedData {
    version: number;
    id: string;
    title: string;
    price: number;
    userId: string;
    orderId?: string;
}
export declare class TicketUpdatedEvent extends Event<TicketUpdatedData> {
    eventName: EventNames;
}
