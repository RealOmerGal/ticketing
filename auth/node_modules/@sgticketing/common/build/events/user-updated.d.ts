import { Event } from './event';
import { EventNames } from './event-names';
export interface UserUpdatedData {
    version: number;
    id: string;
    email: string;
    role: string;
}
export declare class UserUpdatedEvent extends Event<UserUpdatedData> {
    eventName: EventNames;
}
