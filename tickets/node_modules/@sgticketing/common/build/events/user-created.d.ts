import { Event } from './event';
import { EventNames } from './event-names';
export interface UserCreatedData {
    id: string;
    email: string;
}
export declare class UserCreatedEvent extends Event<UserCreatedData> {
    eventName: EventNames;
}
