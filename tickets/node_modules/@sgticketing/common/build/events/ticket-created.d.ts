import { Event } from './event';
import { EventNames } from './event-names';
export interface TicketCreatedData {
    version: number;
    id: string;
    title: string;
    price: number;
    userId: string;
}
export declare class TicketCreatedEvent extends Event<TicketCreatedData> {
    eventName: EventNames;
}
