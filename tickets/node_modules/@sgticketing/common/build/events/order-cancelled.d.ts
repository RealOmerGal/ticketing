import { Event } from './event';
import { EventNames } from './event-names';
export interface OrderCancelledData {
    id: string;
    version: number;
}
export declare class OrderCancelledEvent extends Event<OrderCancelledData> {
    eventName: EventNames;
}
